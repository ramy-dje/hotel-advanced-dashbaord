// the food ingredient interface
export default interface FoodIngredientInterface {
  name: string;
  createdAt: string | Date;
  id: string;
}
// the create food ingredient interface
export interface CreateFoodIngredientInterface {
  name: string;
}
