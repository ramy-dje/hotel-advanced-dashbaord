// the blog category interface
export default interface RatePlanCategoryInterface {
    name: string;
    createdAt: string | Date;
    id: string;
  }
  // the create blog category interface
  export interface CreateRatePlanCategoryInterface {
    name: string;
  }
  