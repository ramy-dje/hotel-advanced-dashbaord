// the blog tag interface
export default interface BlogTagInterface {
  name: string;
  createdAt: string | Date;
  id: string;
}
// the create blog tag interface
export interface CreateBlogTagInterface {
  name: string;
}
