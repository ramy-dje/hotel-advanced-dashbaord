// the room includes interface
export default interface RoomIncludesInterface {
  name: string;
  createdAt: string | Date;
  id: string;
}

// the create room includes interface
export interface CreateRoomIncludesInterface {
  name: string;
}
